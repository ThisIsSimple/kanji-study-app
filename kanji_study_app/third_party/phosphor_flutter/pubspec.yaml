name: phosphor_flutter
description: Local compatibility shim for the subset of Phosphor icons used by this app.
version: 2.1.0
publish_to: none

environment:
  sdk: ">=3.8.1 <4.0.0"
  flutter: ">=3.35.0"

dependencies:
  flutter:
    sdk: flutter
