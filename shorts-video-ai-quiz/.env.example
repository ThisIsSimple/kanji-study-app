# Quiz Shorts Video Generator 환경 변수

# ===== 서버 설정 =====
HOST=0.0.0.0
PORT=8080

# ===== 디버그 저장 설정 =====
# 영상 저장 활성화 (true/false)
DEBUG_SAVE_VIDEO=false

# 저장소 유형 (local/gcs)
STORAGE_TYPE=local

# 로컬 저장 경로 (STORAGE_TYPE=local일 때)
OUTPUT_DIR=./output

# ===== Google Cloud Storage 설정 (STORAGE_TYPE=gcs일 때) =====
# GCS 버킷 이름
GCS_BUCKET=your-bucket-name

# GCS 인증 (서비스 계정 키 파일 경로)
# Cloud Run에서는 자동으로 설정됨
# GOOGLE_APPLICATION_CREDENTIALS=./gcs-credentials.json
